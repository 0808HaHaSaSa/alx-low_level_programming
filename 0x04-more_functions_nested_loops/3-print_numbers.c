#include <stdio.h>
/**
*print_numbers - checks for a digit
*
*description:checks for a digit (0 through 9).
*Return:  always O
*/
void print_numbers(void)
{
int i;
for (i = 0; i < 9; i++)
printf("%d", i);
printf("9\n");
}
