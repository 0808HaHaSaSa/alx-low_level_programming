#include <stdio.h>
#include <ctype.h>
/**
*mul - do multiplies two integers
*@a: it is the argurments
*@b: it is the argument
*description:do multiplies two integers
*Return:  return a*b
*/
int mul(int a, int b)
{
return (a * b);
}
