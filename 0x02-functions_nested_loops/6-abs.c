#include <stdio.h>
#include <ctype.h>
#include <math.h>
#include <stdlib.h>
/**
**_abs - the sum of all its parameters.
*@n:it the argument
*description: the sum of all its parameters.
*Return:  always 0
*/
int _abs(int n)
{int a;
a = abs(n);
return (a);
}
