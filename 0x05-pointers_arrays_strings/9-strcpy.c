#include <stdio.h>
#include <string.h>
/**
*_strcpy - prints andd copies the string
*@dest:it the argument
*@src:it the argument
*description: prints n elements of an array of integers
*Return:  always 0
*/
char *_strcpy(char *dest, char *src)
{
strcpy(dest, src);
return (dest);
}
