#include <stdio.h>
#include <string.h>
/**
*_strlen - the length of a string.
*@s:it the argument
*description:the length of a string.
*Return:  always the length
*/
int _strlen(char *s)
{
int n;
n = strlen(s);
return (n);
}
