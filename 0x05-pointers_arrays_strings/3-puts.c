#include <stdio.h>
#include <string.h>
/**
*_puts - prints a string, followed by a new line
*@str:it the argument
*description:prints a string, followed by a new line
*Return:  always 0
*/
void _puts(char *str)
{
puts(str);
}
